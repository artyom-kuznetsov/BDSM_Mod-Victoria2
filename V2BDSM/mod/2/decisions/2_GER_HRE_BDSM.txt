political_decisions = {
    form_germania = {
        potential = {
            NOT = {
                exists = GER
                exists = HLR
            }
			NOT = { has_country_flag = hide_all_decs_flag }
            OR = {
                primary_culture = south_german
                primary_culture = north_german
                primary_culture = rhenish
				tag = ELB
				tag = BOH
            }
        }
        
        allow = {
            pan_nationalism_tech = 1
            GER = {
                all_core = {
                    OR = {
                        owned_by = THIS
                        owner = { OR = { in_sphere = THIS vassal_of = THIS } }
                    }
                }
            }
        }

        effect = {
            prestige = 50
			badboy = 10
            add_accepted_culture = rhenish
            add_accepted_culture = north_german
            add_accepted_culture = south_german
            change_tag = GER
			random_owned = {
				limit = {
					owner = {
						primary_culture = czech
					}
				}
				owner = { primary_culture = czech CUSTOM_TOOLTIP_GER_BOH = { } }
			}
            any_country = {
                limit = {
                    OR = {
                        primary_culture = south_german
                        primary_culture = rhenish
                        primary_culture = north_german
                    }
					OR = {
						in_sphere = THIS
						vassal_of = THIS 
					}
                }
				any_owned = {
					# Audax Validator "." Ignore_NEXT
					province_selector = -1
					add_core = THIS
				}
                country_event = { id=11101 days=0 }
            }
        }
    }
}